#pragma once


#include <iostream>
#include <algorithm>
#include <cstdlib>
#include <vector>
#include <random>
#include <cstring>

const double BASE_SIZE = 1.0 / 60;
const double BASE_SPEED = 1.0 / 60;
const int NEIGHBOUR_SIZE = 10;

const std::string DataFile = "train.data";
const std::string NetFile = "train.net";
